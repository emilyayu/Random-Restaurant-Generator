# import socket
# import random

# host = 'localhost'
# port = 1305
# # create a socket at client side using TCP / IP protocol
# with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:
#     number = random.randint(1, 20)
#     s.connect((host, port))
#     s.send(str(number).encode(encoding='UTF-8', errors='strict'))
#     # Receives restaurant number
#     msg = s.recv(1024)
# print(str(msg))

print('PYTHON PAGE')